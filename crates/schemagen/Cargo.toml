[package]
name = "schemagen"
version = "0.1.0"
edition = "2024"
publish = false

[build-dependencies]
dprint-plugin-kdl = { path = "../..", features = ["schema"] }

[dev-dependencies]
jsonschema = { version = "0.58.0", default-features = false }
serde_json = { version = "1.0" }
